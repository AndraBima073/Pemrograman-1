#include <stdio.h>

int main() {
    double jarak = 150;
    double jam = 2.5;
    double kecepatan;

    kecepatan = jarak / jam;
    printf("Kecepatan rata-rata = %.2f km/jam\n", kecepatan);

    return 0;
}